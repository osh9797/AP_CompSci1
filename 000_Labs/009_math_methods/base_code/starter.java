/*
 *	Author:  
 *  Date: 
*/

import java.util.Scanner;

class starter {
	public static void main(String args[]) {
		// the string "I love to learn coding remotely." will appear in
		// the command window when you compile and run this program.
		int one = (13-6*11);
		int two = (30%7*(-2));
		System.out.println("The larger number out of " + one + " and " + two + " is " + Math.max(one,two) );
		int root = (3*8+31%7);
		System.out.println("The square root of " + root + " is " + Math.sqrt(root) );
		double power = (37.0/3.0);
		int exp = (35%21);
		System.out.println("The number " + power + " to the power of " + exp + " is " + Math.pow(power,exp) );
		int pow2 = 2;
		int exp2 = 14%3;
		int root2 = 2*6;
		System.out.println("The largest number out of 2 to the power of " + pow2 + " and the square root of " + root2 + " is " + Math.max(Math.pow(pow2,exp2),Math.sqrt(root2)));
		
		
		
		
		
		
		
		
	}
}
